# Exclude the local node_modules directory from the build context. Dependencies
# are installed inside the container via npm install, so there's no need to copy
# your local modules. This reduces the upload size and speeds up builds.
node_modules

# Ignore debug log files.
npm-debug.log*

# Ignore the data directory to avoid copying local databases or uploaded files
# into the container. At runtime the application will create this directory if
# it does not exist. If you commit a `.gitkeep` inside data for version control,
# add an exception below.
data/
!data/.gitkeep